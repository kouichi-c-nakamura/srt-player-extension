{
  "manifest_version": 3,
  "name": "SRT Player (Independent Timer)",
  "version": "0.1.0",
  "description": "Step 1: Load and parse an SRT file, verify output before building the overlay/timer UI.",
  "action": {},
  "permissions": [],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["srt-parser.js", "overlay.js"],
      "css": ["overlay.css"],
      "run_at": "document_idle"
    }
  ]
}
