{
  "manifest_version": 3,
  "name": "SRT Player (Independent Timer)",
  "version": "0.2.0",
  "description": "Floating SRT subtitle overlay with an independent, manually-synced timer. Works the same on any site since it never touches the page's video element.",
  "action": {},
  "permissions": ["storage"],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["srt-parser.js", "overlay.js"],
      "css": ["overlay.css"],
      "run_at": "document_idle"
    }
  ],
  "browser_specific_settings": {
    "gecko": {
      "id": "srt-player@example-local-dev.invalid",
      "strict_min_version": "109.0"
    }
  }
}
